# 2026-08-11

## react-best-practices-fix 协作（fixer-shared-gto-hh）
- 完成 `.sort()` → `.toSorted()` 不可变排序重构，共修改 9 个文件 13 处：
  - src/shared/utils/handRanking.ts（4 处：L85/L97/L162/L202）
  - src/shared/utils/persistShape.ts（L35，Object.keys(...).toSorted()）
  - src/shared/utils/seededShuffle.ts（L100）
  - src/features/gto-simulator/utils/boardGenerator.ts（L19）
  - src/features/gto-simulator/utils/postflopStrategy.ts（L59）
  - src/features/gto-simulator/utils/strategyCompare.ts（L132 链式 + L330 高风险）
  - src/features/gto-simulator/store.ts（L258）
  - src/features/hand-history/utils/gtoDeviation.ts（L67）
  - src/features/hand-history/store.ts（L426 高风险）
- 高风险项处理：
  - strategyCompare.ts L330：`actions` 为 const，改用 `const sortedActions = actions.toSorted(...)` 并更新后续 `actions[0]` → `sortedActions[0]`
  - hand-history/store.ts L426：`result` 为 let 且后续 return，用 `result = result.toSorted(...)` 原地重新赋值
- 所有文件 lint 0 错误。未触碰任何 *.test.ts / *.test.tsx。

## react-best-practices-fix 协作（fixer-academy-puzzle-range）
- 完成 `.sort()` → `.toSorted()` 不可变排序重构，共修改 6 个文件 10 处：
  - src/features/strategy-academy/components/ConceptGraph.tsx（6 处副本排序，直接改方法名）
  - src/features/strategy-academy/utils/adaptiveDifficulty.ts（L70 高风险，filter 链上接 toSorted）
  - src/features/strategy-academy/utils/practiceOptionOrder.ts（L112 高风险，map 链上接 toSorted）
  - src/features/puzzle-trainer/data/rushQuestions.ts（L47 高风险，数组字面量接 toSorted，并删除遗留的 picked.sort 行）
  - src/features/puzzle-trainer/utils/optionOrder.ts（L88 高风险，map 链上接 toSorted）
  - src/features/range-trainer/components/SessionResult.tsx（L36 链式）
- 高风险项统一策略：目标变量均为 const 且排序后需继续使用，采用「把 `.toSorted()` 接到新数组构造链（filter/map/数组字面量）上」的写法，无需新增临时变量、不改后续使用点。
- 教训：rushQuestions.ts 初次编辑加了 toSorted 却漏删原位置 picked.sort，造成重复排序+原地突变；读取确认后已删除遗留行。
- 所有文件 lint 0 错误。未触碰任何 *.test.ts / *.test.tsx。

## i18n 首页星期标签修复
- `src/features/progress/components/streak/StreakRail.tsx` 中硬编码中文星期数组 `['一', '二', ...]` 替换为通过 i18n 解析的 `dashboard.streakRail.weekDays.{mon..sun}`（zh/en 双语均已有对应 key）。
- 注意：`useMemo` 依赖数组需显式列出 `weekDayLabels`；当 `weekData` 回调不使用 label 时改用 `(_label, i)` 命名避免 TS 严格模式 unused-parameter 报错。
- pnpm verify（typecheck + lint + 553 tests）全绿。

## GTO 场景 name 硬编码中文修复（首页 Training Plan 中英混杂）
- 根因：src/features/gto-simulator/utils/scenarioGenerator.ts L276 模板字符串在英文界面下产生 "BTN Turn 决策" 中英混杂。该 name 会被 useGTOComparison.ts L268 写入 SRS ReviewItem.label，然后在首页 DailyTrainingPlan review 推荐里以 item.label 形式渲染。

## React 最佳实践修复（主代理收尾，团队协作完成）
- **tsconfig**：`tsconfig.json` / `tsconfig.app.json` 的 `lib` ES2020 → ES2023（target 保持 ES2020，构建产物零影响），解锁 toSorted/toReversed/toSpliced 类型定义
- **P1**：26 个生产文件 39 处 `.sort()` → `.toSorted()` 全部完成（3 个团队 fixer 按模块并行：shared/gto/hand-history、progress、academy/puzzle/range），7 处高风险源数组突变均正确修复，测试文件保留 .sort()
- **P3**：PracticeDrill.tsx 重构两个 Effect：
  - 删除压力模式难度递增 Effect（currentDifficulty state 在压力模式下无消费者，checkDifficultyAdjustment 有 isPressure 守卫）
  - 超时处理从独立 Effect（监听 timeRemaining===0）改为倒计时 interval tick 内直接触发 handleTimeout（useCallback），消除 Effect 链
  - 新增 timeRemainingRef / timeoutFlashTimerRef 两 ref，卸载 cleanup 同步清除超时闪烁 timer
- **验证**：pnpm verify 全绿（typecheck + lint + 76 files / 553 tests）；ui-visual-validator 确认无交互回归（超时单次触发、倒计时/答题/去复习/压力模式均正常）
- CHANGELOG.md 已记录
- 注意：dev server 仍运行于 http://localhost:5173/dezhou/（用户选择保留，dev-server.log 在工作区根，勿删）
- 修复：
  1. scenarioGenerator.ts：移除硬编码中文"决策"，name 改为纯英文结构化标识（position + streetLabel）。
  2. progress store.ts：persist version v10→v11，migrate 中清洗 reviewItems label 尾部 " 决策" 后缀，清理已持久化在 localStorage 的历史脏数据。
- pnpm verify 全绿。

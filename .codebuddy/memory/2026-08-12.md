# 2026-08-12

## 未消费 i18n 模块清理（CHANGELOG 遗留项落地）

- **任务**：清理 7 个未消费保留 i18n 模块（`adaptive`/`app`/`dailyPlan`/`localTrack`/`opponent`/`opponentDrill`/`toast`），模块数 38 → 31。
- **清理依据**：全量实证扫描无任何 `t('<module>.')` 静态 key、动态模板 key、JSON import 消费。注意 `opponent` 模块与数据字段 `scenario.opponent.*` 同名但非 i18n key；`toast` 为组件调用约定非翻译模块；`dailyPlan` 消费实为 `academy.dailyPlan.*` 嵌套键。
- **改动文件**：
  - 删除 `src/i18n/locales/{zh,en}/` 下 14 个 JSON 文件。
  - `src/i18n/moduleRegistry.ts`：从 `I18nModuleKey`/`ALL_MODULES`/`loadModule` 移除 7 key，`ALL_MODULES` 简化为单一活跃清单。
  - `src/i18n/preload.test.ts`：未消费断言改为 `toEqual([])`。
  - `docs/CHANGELOG.md`：新增「未消费 i18n 模块清理」小节。
- **验证**：`pnpm typecheck` 0 错误、`pnpm lint` 0 错误、`pnpm test` 76 files / 546 tests 全通过。

## 重要环境提示（用户告知）

- **执行环境删除文件有 bug**：`delete_file` 工具与批量 `Remove-Item`（数组传参/相对路径）会返回 success 但**实际不删除文件**。需用绝对路径 + 循环单个删除（如 `Remove-Item -LiteralPath <绝对路径> -Force -ErrorAction Stop`）才真正生效。验证方式：删除后 `Test-Path` 必须为 `False`。

## CHANGELOG 仍存遗留问题（供后续参考）

- **数据层正文内容硬编码中文**（strategy-academy/theory-academy 的 content/handExamples/quiz 等）：英文环境混排中文，CHANGELOG 多次标记"本批不覆盖"，是最明确的实质遗留项。
- **i18n 第三语言**：计划性遗留，新增第三语言时评估 i18next-parser。

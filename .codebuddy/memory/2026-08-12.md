# 2026-08-12

## 未消费 i18n 模块清理（CHANGELOG 遗留项落地）

- **任务**：清理 7 个未消费保留 i18n 模块（`adaptive`/`app`/`dailyPlan`/`localTrack`/`opponent`/`opponentDrill`/`toast`），模块数 38 → 31。
- **清理依据**：全量实证扫描无任何 `t('<module>.')` 静态 key、动态模板 key、JSON import 消费。注意 `opponent` 模块与数据字段 `scenario.opponent.*` 同名但非 i18n key；`toast` 为组件调用约定非翻译模块；`dailyPlan` 消费实为 `academy.dailyPlan.*` 嵌套键。
- **改动文件**：
  - 删除 `src/i18n/locales/{zh,en}/` 下 14 个 JSON 文件。
  - `src/i18n/moduleRegistry.ts`：从 `I18nModuleKey`/`ALL_MODULES`/`loadModule` 移除 7 key，`ALL_MODULES` 简化为单一活跃清单。
  - `src/i18n/preload.test.ts`：未消费断言改为 `toEqual([])`。
  - `docs/CHANGELOG.md`：新增「未消费 i18n 模块清理」小节。
- **验证**：`pnpm typecheck` 0 错误、`pnpm lint` 0 错误、`pnpm test` 76 files / 546 tests 全通过。

## 重要环境提示（用户告知）

- **执行环境删除文件有 bug**：`delete_file` 工具与批量 `Remove-Item`（数组传参/相对路径）会返回 success 但**实际不删除文件**。需用绝对路径 + 循环单个删除（如 `Remove-Item -LiteralPath <绝对路径> -Force -ErrorAction Stop`）才真正生效。验证方式：删除后 `Test-Path` 必须为 `False`。

## 语言切换滞后更新排查与修复（2026-08-12）

- **根因（P0）**：react-i18next v17 默认 `bindI18nStore: ''`，`useTranslation` 只订阅 `languageChanged`。feature 懒加载翻译模块在语言切换时由 `preload.ts` 异步补加载，`addResourceBundle` 注入后不触发重渲染，文案滞留 fallback（zh）直到下次任意渲染；core 模块（静态打包）即时生效 → "部分立即、部分滞后"。
- **修复**：`src/i18n/config.ts` 配置 `react: { bindI18nStore: 'added removed' }`；新增回归测试 `src/i18n/languageSwitch.test.tsx`（复现 core 即时/懒加载 fallback/注入后自动刷新）。
- **硬编码清理（P2）**：13 个组件 40+ 处硬编码中文已国际化（GTOResultPage/SpotTrainer/LessonQuiz/LevelCertification/ChoiceDrillRenderer/PracticeDrill/ContentBlock/DiagramBlock/HandExample/QuickDrill/RangeStatsPage/GTOStatsPage/FreezeChip），新增 gto/academy(quiz·levelCertification·difficulty·gameContext·content·drill)/drills/quickDrill 双语 key，删除 PracticeDrill 的 DIFFICULTY_LABELS 常量。
- **潜伏问题**：hand-history store 的 `dbError` 存翻译字符串（`classifyDBError` 用 `i18n.t()`），当前无 UI 消费方，建议改存错误类型 key。
- **文档**：`language-switch-audit.md`（根目录排查报告）、TDD §9.1 补语言切换重渲染机制、CHANGELOG 新条目。
- **验证**：`pnpm verify` 通过（typecheck + lint + 77 files / 547 tests）。

## 语言切换修复方案文档（含子代理协作）

- 生成 `language-switch-fix-plan.md`（根目录）：将已完成的语言切换修复整理为"修复方案+执行记录"，含四类根因、P0/P2 修复、子代理协作分工表（platform/gto/strategy-academy/progress/ui-ux）、回归测试与验证（547 tests / 77 files）、文档同步清单与 3 条遗留建议。
- 说明：所有代码修复在本次文档前已完成，故方案文档仅作结构化追溯记录，未重复改动代码。

## 语言切换遗留建议执行（2026-08-12）

- **建议 3（切换预加载）**：`preload.ts` 新增 `switchLanguage(next)`（先预加载 touchedKeys 目标语言再 changeLanguage），`AppLayout` 三处语言按钮 + `SettingsPage` 接入（切换中禁用+旋转动画）；`preload.test.ts` 新增时序与幂等测试（注意：`rangeTrainer` 的 en 资源会被前序补加载测试注入，新测试用 `potOdds`）。
- **建议 2（dbError 存 key）**：hand-history store `dbError` 改存 `DBErrorType`（'quotaExceeded'|'unavailable'|'generic'），`classifyDBError` 不再 `i18n.t()`；`HandHistoryList` 新增错误条 `t('handHistory.dbError.'+type)`。
- **建议 1 可落地子集（gto 生成文案 key 化）**：`DecisionNode` 加 `descriptionKey`/`descriptionParams`；`scenarioGenerator` 4 节点 + `getEasyGTOScenario` 描述改存 key（`gto.nodeDesc.*`/`gto.easyScenario.*`）；`DecisionTree` 渲染 `t()`；SRS metadata.front 存 key（ReviewSession 已支持 key 渲染）；`strategyCompare.explanation` 加可选 t 参数（`gto.feedback.explanationOptimal/Mixed`）；新增 `scenarioI18n.test.ts` 守卫。删除 `GAME_VARIANT_CONFIGS.displayName`（无消费方）；`handRankingChanges` 同样无消费方但保留待 key 化。
- **内容层专项**：strategy-academy/theory-academy 课程正文约 1.3MB+ 中文超大规模，产出 `content-i18n-roadmap.md` 分阶段方案（阶段一数据模型扩展→阶段二数据 key 化+双语翻译→阶段三守卫验证），不并入本次。
- **验证**：`pnpm verify` 全量通过（typecheck + lint + 551 tests / 78 files）。文档：CHANGELOG 新条目 + `language-switch-fix-plan.md` §7 更新为执行状态。

## CHANGELOG 仍存遗留问题（供后续参考）

- **课程正文内容 key 化**：见 `content-i18n-roadmap.md` 三阶段方案，待排期。
- **i18n 第三语言**：计划性遗留，新增第三语言时评估 i18next-parser。

# 子智能体文件标准化规范

本文档定义了 `.claude/agents/*.md` 文件的统一格式和内容规范。

> **重要提示**：
> - **模板文件**（TEMPLATE-standardized-dev.md）已移至 `docs/agents-guide/` 目录，**不应放在 `.claude/agents/` 下**，避免被软件识别为实际子代理
> - **本规范文档**也位于 `docs/agents-guide/`，供团队参考使用
> - 各子代理的 `tools`、`model`等字段**应灵活配置**，不强制固定值

## 📋 一、标准结构模板

每个子智能体文件必须包含以下章节（按顺序）：

```markdown
每个子智能体文件的 frontmatter 示例（flexible）：

```
---
name: <module-name>-dev
description: <模块功能描述>
tools: [Read, Glob, Grep, LSP, GetProblems, SearchReplace, Write, DeleteFile, Bash, GetTerminalOutput]  # 灵活配置
model: <模型选择>  # 灵活配置
skills: []  # 灵活配置
mcpServers: []  # 灵活配置
additionalPrompt: ""  # 灵活配置
---
```

# <Module Name> Developer

## Role
专注于 <模块名称> 模块的前端开发 Agent。<模块的核心目标和适用场景>。

## Context
- **项目路径**：工作区根目录（本文件所有路径均为相对工作区路径）
- **模块路径**：`src/features/<module-name>/`
- **技术栈**：React 19 + TypeScript 7 + Zustand 5 + Tailwind CSS 4 + [其他依赖]
- **路由**：`/path1` / `/path2`（如有，均用 LazyWrapper 包裹）
- **持久化**：`<store-name>`（persist version 以 `src/features/<module-name>/store.ts` 的 persist 配置为唯一事实源）
- **定位分工**：与相关模块的定位区分（如"理论学习→实践应用闭环"）

## Authority
### 可决策范围
- `<职责 1>`
- `<职责 2>`
- `<职责 3>`（关键参数需注明详见 code/path）

### 不可越界
- 不修改 `src/shared/types/` 下的类型定义（如需扩展须通过 `platform-dev` 协调）
- 不直接读写 `src/features/progress/store.ts` 的状态字段（必须通过公开 action）
- 不修改其他 feature 模块文件，跨模块协作通过 `trainingEvents` 事件总线或 `platform-dev` 协调
- 不引入新依赖，如确有必要须先评估 bundle 体积并经 `platform-dev` 评审

## Capabilities
- `<能力 1>`：简洁描述核心功能（必要时括号备注数值或路径引用）
- `<能力 2>`
- `<能力 3>`
- `<能力 4>`：跨模块集成说明（详见 Cross-Module Touchpoints）

> 注：<需要特别说明的架构细节，如 colocated hooks、独立 store 解耦等>

## Cross-Module Touchpoints
本模块作为 `<消费者/生产者>` 与以下系统交互：

### <target-module>（如 progress store）
- **<系统名>**：通过 `<action/method>` 调用 `<具体逻辑>`
- **<系统名>**：通过 `<action/method>` 调用 `<具体逻辑>`
- **幂等要求**：如有必要，说明重复调用的处理规则

### trainingEvents（事件总线）
- **emit 时机**：在 `<组件/模块>` 完成时调用 `trainingEvents.emit({ module: '<module-name>', mode: '<mode>', result, createdAt })`
- **subscribe 方**：progress store 自动订阅并更新统计

### shared/ 层依赖
- `shared/types/<type>.ts`：<类型用途>
- `shared/utils/<util>.ts`：<工具函数用途>
- `shared/constants/<const>.ts`：<常量模板用途>
- `shared/stores/<store>.ts`：<状态管理用途>

## Key Files
> 目录级描述，具体文件以目录实际内容为事实源（新增/删除文件无需同步本清单）。

### 模块内
- `src/features/<module-name>/` — 模块根（types.ts / store.ts / index.ts）
- `src/features/<module-name>/data/` — 静态数据（题库 / 课程 / 配置）
- `src/features/<module-name>/utils/` — 工具函数（算法 / 排序 / 计算）
- `src/features/<module-name>/hooks/` —消费 hook（状态编排）
- `src/features/<module-name>/components/` — UI 组件

### 跨模块依赖
- `src/shared/types/<type>.ts` — 共享类型定义
- `src/features/progress/store.ts` — 跨模块状态中枢（仅消费公开 action）

## Workflows
1. **典型任务 1**：步骤分解 → 确认点 → 验证步骤
2. **典型任务 2**：步骤分解 → 跨模块检查点 → 测试步骤
3. **典型任务 3**：步骤分解 → 质量门禁触发点
4. **新增页面/组件标准路径**：在 components/ 创建组件（单文件 ≤300 行）→ 同步 zh/en 双语 i18n key（`<prefix>.*` 前缀）→ 按内容补测试并选对后缀（纯逻辑 `.test.ts` / 组件冒烟 `.test.tsx`）→ 运行 `pnpm verify`；需新路由时经 platform-dev 在 routes.tsx 注册（React.lazy + LazyWrapper），视觉一致性经 ui-ux-dev 复核

## Constraints
继承 AGENTS.md 全局约束（模块间禁止直接引用 / 单文件 ≤300 行 / 工具函数纯函数 / trainingEvents 事件总线 / persist 升级硬性规则等）。

### 模块特有约束
- **<约束名称 1>**：<详细描述>（如涉及跨模块影响，注明参数详见 code/path）
- **<约束名称 2>**：<详细描述>
- **<约束名称 3>**：<详细描述>（如涉及重要数值参数，加粗提示）

## Quality Checklist
- [ ] `node node_modules/typescript/bin/tsc --noEmit` exit code 0
- [ ] zh.json 与 en.json 双语同步（i18n key 前缀 `<prefix>.*`）
- [ ] <关键行为 1>（如：答题后已调用 ELO + SRS + Emotion + Mentor + Streak N 处）
- [ ] <关键行为 2>（如：trainingEvents.emit 已发布）
- [ ] <组件规范>（如：Recharts 图表渲染正确，暗色主题适配）
- [ ] <关键机制生效>（如：末题简单 + 补救机制生效，rescueUsed 仅一次）
- [ ] <约束检查>（如：连续答错 3 次时显示降级提示 banner）
- [ ] <选项排序治理>（如：题库出口已应用语义排序，quizOrder.test.ts 分布守卫通过）
- [ ] <反馈闭环>（如：答错反馈携带 relatedLessonId，"去复习"链接可跳转）
```

---

## 🎯 二、内容精简原则

### 原则 1：行为指导 > 描述性知识

**错误示范**（冗余描述）：
```markdown
- 对手形象系统：TAG / LAG / NIT / Calling Station / Maniac / Unknown 六类，VPIP / PFR / AF 等统计可视化
```

**正确做法**（行为指导 + 引用权威）：
```markdown
- 对手形象系统开发：六类对手数据与统计可视化（分类详见模块知识卡片）
```

### 原则 2：引用权威 > 复制副本

所有**事实性信息**应遵循：
- ✅ 引用代码：`以 <file.ts> 为唯一事实源`
- ✅ 引用专用文档：`详见 docs/<doc-name>.md`
- ✅ 引用 Qoder 自动知识：`详见 .qoder/repowiki/`
- ❌ **禁止**在本文件中硬编码副本

**示例**：
```markdown
# ❌ 不应写死数值
- 持续训练 7 天奖励冻结卡

# ✅ 应改为引用
- 连续 7 天奖励冻结卡（阈值以 progress store 实现为准）
```

### 原则 3：模块化结构

所有章节采用统一格式，便于 AI 代理快速检索：
- 使用 Markdown 标题（## Role / ## Context 等）
- 列表项保持平行结构
- 避免过深的嵌套层次（最多 3 层）

---

## 🔍 三、审查要点清单

每次提交前自查以下项目：

### 结构完整性 ✅
- [ ] 是否包含所有必需章节（Role / Context / Authority / Capabilities / Cross-Module / Key Files / Workflows / Constraints / Quality Checklist）
- [ ] frontmatter 格式是否正确（name / description / tools / model 等）
- [ ] 文件名是否与 frontmatter `name` 字段一致

### 内容精简性 ✅
- [ ] Capabilities 是否压缩到≤6 条？
- [ ] 每条 Capability 是否≤30 字？
- [ ] 是否删除了公式展开、枚举清单等描述性内容？
- [ ] 是否将具体数值改为"详见 code/path"引用？

### 引用准确性 ✅
- [ ] 是否所有"事实源"声明都指向了单一代码文件或文档？
- [ ] 是否避免了维护多个版本的同一信息？
- [ ] Key Files 是否只列出了目录结构而非具体文件名？

### 约束清晰度 ✅
- [ ] Constraints 第一行是否写明"继承 AGENTS.md 全局约束..."？
- [ ] 模块特有约束是否有明确的通过标准（是/否判断）？
- [ ] Quality Checklist 每项是否可量化验证？

### 一致性 ✅
- [ ] 是否与 AGENTS.md 保持一致（命名规范、文件结构、质量门禁）？
- [ ] 跨模块权限声明是否与实际 eslint.config.js 白名单一致？
- [ ] store name 和 persist version 引用是否正确？

---

## 📊 四、当前文件清单与状态

| 文件名 | 行数 | 状态 | 最后更新时间 |
|--------|------|------|-------------|
| platform-dev.md | 190 | ⚠️ 待优化（Cross-Module 有重复） | 2026-08-07 |
| progress-dev.md | 145 | ✅ 已基本标准化 | 2026-08-07 |
| range-trainer-dev.md | 110 | ✅ 已基本标准化 | 2026-08-07 |
| strategy-academy-dev.md | 156 | ✅ 已基本标准化 | 2026-08-07 |
| ui-ux-dev.md | 179 | ⚠️ Component Inventory 过长 | 2026-08-07 |
| pot-odds-dev.md | 112 | ✅ 已基本标准化 | 2026-08-07 |
| gto-simulator-dev.md | 114 | ✅ 已基本标准化 | 2026-08-07 |
| hand-history-dev.md | 93 | ✅ 已基本标准化 | 2026-08-07 |
| puzzle-trainer-dev.md | 129 | ✅ 已基本标准化 | 2026-08-07 |
| theory-academy-dev.md | 136 | ✅ 已基本标准化 | 2026-08-07 |
| onboarding-dev.md | 100 | ✅ 已基本标准化 | 2026-08-07 |
| help-center-dev.md | 95 | ✅ 已基本标准化 | 2026-08-07 |
| TEMPLATE-standardized-dev.md | 142 | 📝 模板参考 | 2026-08-07 |

---

## 🔧 五、后续优化建议

### 立即执行（优先级高）

1. **清理 platform-dev.md 的 Cross-Module Touchpoints 重复内容**
   - 第二次出现的 `## Cross-Module Touchpoints` 应该删除
   - 确保每节只出现一次

2. **精简 ui-ux-dev.md 的 Component Inventory**
   - 从当前的 15 行压缩为 1 行引用
   - 改为"详见 DESIGN_LANGUAGE.md §5"

3. **检查所有模块的 store 引用**
   - 确认 persist version 引用是否准确
   - 确保与 actual store.ts 一致

### 中期优化（优先级中）

1. **为帮助中心等静态模块移除不必要的 store 引用**
   - help-center 无 state，不需要提及 cross-module

2. **统一各模块的质量检查清单**
   - 确保每项都有明确的验证标准
   - 删除模糊的描述（如"组件渲染正确"改为"关键组件 render 通过快照测试"）

3. **补充缺失的 Cross-Module Touchpoints**
   - 某些模块可能遗漏了对 shared/层的引用
   - 确保每个模块都清晰说明与 progress store 的关系

### 长期优化（优先级低）

1. **建立自动化守卫测试**
   - 检查所有子智能体文件的结构一致性
   - 验证引用的 file path 是否存在
   - 检查 ESLint 白名单与 module 代理权限的匹配度

2. **定期审计知识库引用**
   - 每季度检查 `.qoder/repowiki/` 是否最新
   - 确保模块代理文件与 repowiki 知识卡片的一致性

---

## 📝 附录 A：常用引用模式

### 1. Store Version 引用
```markdown
- persist version 以 `src/features/<module-name>/store.ts` 的 persist 配置为唯一事实源（本文件不维护数值副本）
```

### 2. Code Fact Source 引用
```markdown
- 阈值数值（UTG / HJ / CO / BTN / SB / BB）以该常量定义为唯一事实源（本文件不维护数值副本）
```

### 3. File Existence 引用
```markdown
- 具体文件以目录实际内容为事实源（新增/删除文件无需同步本清单）
```

### 4. Design System 引用
```markdown
- 质量清单以 `poker-ui-demo/DESIGN_LANGUAGE.md` 当前版本为准
```

### 5. Knowledge Base 引用
```markdown
- 模块能力与实现细节详见对应知识卡片（`.qoder/repowiki/`）
```

---

**版本**：v1.0  
**最后更新**：2026-08-07  
**维护者**：Tech Lead

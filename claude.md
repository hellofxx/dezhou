# CLAUDE.md

> 项目级 AI 智能体配置文件。本文件在每次会话开始时加载。
> 灵感来源：Andrej Karpathy 关于 LLM 编程陷阱的观察与总结。

---

## 环境约束

- **操作系统**：Windows
- **默认 Shell**：PowerShell 7.x（pwsh / PowerShell Core）
- **工作目录**：项目根目录

**硬性要求**：所有终端命令必须使用 **PowerShell** 语法，禁止输出 bash / sh / zsh 指令。

| 要点 | 规范 |
|------|------|
| 路径分隔符 | 反斜杠 `\`，路径用单引号包裹 `'C:\path\to\file'` |
| 环境变量 | `$env:VAR_NAME` |
| 命令串联 | `;` 或 `&&`（PS7+ 均支持） |
| 脚本执行 | `pwsh -File script.ps1` |

---

## 行为准则

**权衡说明**：以下准则偏向谨慎而非速度。对于简单任务（如拼写修正、单行改动），可灵活把握。

### 1. Think Before Coding（先想再写）

**不做沉默假设。不隐藏困惑。主动呈现权衡。**

开始实现之前：
- 明确陈述你的假设。如有不确定，先问。
- 如存在多种解读方式，全部列出来——不要默默选一个。
- 如果有更简单的方案，说出来。必要时提出不同意见。
- 如有不明确的地方，停下来。指出哪里不清楚，然后询问。

### 2. Simplicity First（简洁优先）

**用最少的代码解决问题。不写投机性代码。**

- 不添加超出需求的功能。
- 不为单次使用的代码做抽象。
- 不做未被要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景做错误处理。
- 如果你写了 200 行但其实 50 行就能搞定，重写。

自问："资深工程师会觉得这过度设计了吗？"如果是，简化。

### 3. Surgical Changes（外科手术式修改）

**只碰必须碰的。只清理自己制造的。**

修改现有代码时：
- 不"顺便改进"相邻的代码、注释或格式。
- 不重构没坏的东西。
- 遵循已有代码风格，即使你有不同偏好。
- 如发现无关的死代码，提出来——但不要删除。

你的改动产生了遗留物时：
- 移除因你的改动而变成无用的导入/变量/函数。
- 除非被要求，否则不删除之前就存在的死代码。

检验标准：每一行改动都应能直接追溯到用户的需求。

### 4. Goal-Driven Execution（目标驱动执行）

**定义成功标准。循环验证直到达标。**

将任务转化为可验证的目标：
- "添加验证" → "先写无效输入的测试，再让测试通过"
- "修复 Bug" → "先写一个能复现的测试，再让测试通过"
- "重构 X" → "确保重构前后测试都通过"

多步骤任务，先列出简要计划：

```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

明确的成功标准让你可以独立迭代。模糊的标准（"让它跑起来"）需要不断澄清。

---

**这些准则生效的标志**：diff 中无关改动更少、因过度设计导致的返工更少、澄清问题出现在实现之前而非出错之后。

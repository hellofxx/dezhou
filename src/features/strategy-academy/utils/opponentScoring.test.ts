import { describe, it, expect } from 'vitest';import { scoreOpponentAnswer } from './opponentScoring';import { OPPONENT_DRILL_QUESTIONS } from '../data/opponentProfiles';const question = OPPONENT_DRILL_QUESTIONS[0]!;const wrongType = question.typeOptions.find((id) => id !== question.correctType)!;const wrongStrategyIndex = question.strategyOptions.findIndex(  (_, i) => i !== question.correctStrategyIndex,);describe('scoreOpponentAnswer', () => {  it('两问全对：计为答对', () => {    const result = scoreOpponentAnswer(question, question.correctType, question.correctStrategyIndex);    expect(result).toEqual({ typeCorrect: true, strategyCorrect: true, isFullyCorrect: true });  });  it('半对（类型对/策略错）：不计为答对', () => {    const result = scoreOpponentAnswer(question, question.correctType, wrongStrategyIndex);    expect(result).toEqual({ typeCorrect: true, strategyCorrect: false, isFullyCorrect: false });  });  it('半对（类型错/策略对）：不计为答对', () => {    const result = scoreOpponentAnswer(question, wrongType, question.correctStrategyIndex);    expect(result).toEqual({ typeCorrect: false, strategyCorrect: true, isFullyCorrect: false });  });  it('两问全错：不计为答对', () => {    const result = scoreOpponentAnswer(question, wrongType, wrongStrategyIndex);    expect(result).toEqual({ typeCorrect: false, strategyCorrect: false, isFullyCorrect: false });  });  it('未作答（null）视为答错', () => {    const result = scoreOpponentAnswer(question, null, null);    expect(result).toEqual({ typeCorrect: false, strategyCorrect: false, isFullyCorrect: false });  });});
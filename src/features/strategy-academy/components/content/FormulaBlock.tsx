/**
 * 公式推导块（对外 API 兼容层）：
 * 视觉实现已提升至 shared/components/business/ContentBlocks（theory-academy 共用），
 * 本文件保留原导出名避免扰动既有消费方 import 路径。
 */
export { FormulaBlock, AsciiMonoText } from '@/shared/components/business/ContentBlocks';
